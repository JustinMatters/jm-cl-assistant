{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit*)",
            "command": "uv run ruff check . && uv run ruff format --check .",
            "timeout": 60,
            "statusMessage": "Running ruff lint and format checks..."
          }
        ]
      }
    ]
  }
}
